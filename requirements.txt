# Semantic Topic Mapper — dependencies
# Python 3.10+

# LLM: Google Gemini (gemini-3-flash) via official SDK
google-generativeai>=0.8.0

# Config: load .env from project root
python-dotenv>=1.0.0

# Schema validation for LLM outputs and data models
pydantic>=2.0.0

# Optional / add when implementing exporters:
# graphviz — for cross_reference_graph.pdf
# (stdlib csv, json sufficient for other exports)
